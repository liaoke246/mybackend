<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.MessageBoardMapper">

    <resultMap id="MessageEntityResultMap" type="com.example.demo.model.entity.MessageEntity">
        <id property="id" column="id"/>
        <result property="nickname" column="nickname"/>
        <result property="contact" column="contact"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumnList">
        id, nickname, contact, content, created_at, updated_at
    </sql>

    <select id="selectMessages" resultMap="MessageEntityResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM board_message
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectById" resultMap="MessageEntityResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM board_message
        WHERE id = #{id}
    </select>

    <select id="countMessages" resultType="long">
        SELECT COUNT(1)
        FROM board_message
    </select>

    <insert id="insertMessage" parameterType="com.example.demo.model.entity.MessageEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board_message (nickname, contact, content, created_at, updated_at)
        VALUES (#{nickname}, #{contact}, #{content}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="updateMessage" parameterType="com.example.demo.model.entity.MessageEntity">
        UPDATE board_message
        SET nickname = #{nickname},
            contact = #{contact},
            content = #{content},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM board_message
        WHERE id = #{id}
    </delete>

</mapper>
